# @smartosa/core

Модель дома: устройства, стены камер, визиты, `HomeRepository`. Без React.

`createMemoryRepository` — тесты и будущие адаптеры. Веб сейчас закрывает тот же интерфейс через IndexedDB.
